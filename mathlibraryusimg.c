#include <stdio.h>
#include <math.h>

int main() {
    double num = 16;
    printf("Square root of %.2f is %.2f\n", num, sqrt(num));
    printf("2 raised to power 3 is %.2f\n", pow(2, 3));
    return 0;
}
